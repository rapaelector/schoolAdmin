<form method="post" action="{{ path('q_c_m_delete', {'id': q_c_m.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ q_c_m.id) }}">
    <button class="btn">Delete</button>
</form>
