{% extends 'base.html.twig' %}

{% block title %}QCM{% endblock %}

{% block body %}
    <h1>QCM</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ q_c_m.id }}</td>
            </tr>
            <tr>
                <th>Question</th>
                <td>{{ q_c_m.question }}</td>
            </tr>
            <tr>
                <th>Answer</th>
                <td>{{ q_c_m.answer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('q_c_m_index') }}">back to list</a>

    <a href="{{ path('q_c_m_edit', {'id': q_c_m.id}) }}">edit</a>

    {{ include('qcm/_delete_form.html.twig') }}
{% endblock %}
